🧪 Testes Unitários no .NET
📌 1. O que são Testes Unitários?
Testes unitários são pequenos testes automatizados que verificam se uma unidade de código (geralmente um método ou função) funciona corretamente de forma isolada.
✔️ “Unidade” = o menor pedaço testável da aplicação (ex: um método).
✔️ “Isolado” = sem depender de banco, API externa, arquivos, etc.
✔️ Objetivo = garantir que o código faça exatamente o que foi planejado.
________________________________________
📌 2. Para que servem os Testes Unitários?
🎯 2.1 Garantir que o código funciona
Ajudam a validar a lógica da aplicação logo no começo do processo.
🔄 2.2 Evitar regressões
Quando você altera algo no código, os testes confirmam que nada que já estava funcionando quebrou.
🛠️ 2.3 Facilitar refatorações
Com testes, você pode melhorar o código com mais confiança.
📚 2.4 Servir como documentação viva
O próprio teste explica “como usar” o método, deixando claro quais comportamentos são esperados.
⚙️ 2.5 Automatizar validações
Ao rodar o comando dotnet test, você confirma automaticamente que tudo está funcionando.
________________________________________
📌 3. Por que os Testes Unitários são importantes?
Sem testes unitários, você depende de:
•	Testes manuais repetitivos
•	Risco de bugs silenciosos
•	Dificuldade de manter o código no longo prazo
•	Medo de mexer no código por receio de quebrar algo
Com testes unitários:
•	O software fica mais robusto
•	A manutenção fica mais segura
•	O time ganha agilidade
•	O código se torna mais modular, já que métodos muito grandes são difíceis de testar
________________________________________
🧱 4. Estrutura de Testes Unitários no .NET
O .NET trabalha muito bem com frameworks de testes como:
•	xUnit (mais moderno, muito usado)
•	NUnit
•	MSTest
O mais recomendado hoje é xUnit, especialmente em projetos novos.

📦 Criando um projeto de testes:

dotnet new xunit -n MinhaApi.Tests

🔗 Referenciando o projeto principal:

Rodar na pasta Projeto de estudos:
dotnet add MinhaApi.Tests reference MinhaApi	

Por que o projeto de testes precisa referenciar o projeto principal?
Em termos práticos e do compilador, é por três motivos principais:
1.	Acesso ao código que será testado (resolução de símbolos)
o	Seus testes chamam classes/métodos definidos no projeto principal. Sem a referência, o compilador do projeto de testes não conhece esses tipos (erros como “The type or namespace name ‘X’ could not be found”).
o	A referência cria uma dependência de compilação: o projeto de testes “enxerga” os namespaces, classes e métodos públicos (e os internal se você liberar via InternalsVisibleTo, veja abaixo).
2.	Build e versionamento consistentes
o	Com uma ProjectReference, o dotnet test sabe buildar na ordem certa: primeiro o projeto principal, depois os testes que dependem dele, garantindo que os testes sempre rodem contra o código mais recente.
o	Evita o risco de testar contra um DLL antigo (o que pode acontecer se você adiciona um arquivo .dll estático ao invés de uma ProjectReference).
3.	Descoberta e execução pelo test runner
o	O runner (xUnit/NUnit/MSTest via dotnet test/IDE) carrega o assembly de testes e, por tabela, precisa carregar o assembly do SUT (System Under Test) referenciado para instanciar classes, executar métodos e avaliar asserts.

Colocar no MinhaApi.Tests.csproj

<ItemGroup>
    <PackageReference Include="xunit" Version="2.7.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.5.7">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
  </ItemGroup>

Criar a classe de testes unitários.

Rodar dotnet test.

Para ver a porcentagem da cobertura de testes unitários, primeiro instale o directory:

dotnet test --collect "XPlat Code Coverage" --results-directory TestResults
dotnet add package coverlet.collector
dotnet test --collect:"XPlat Code Coverage"
dotnet tool install -g dotnet-reportgenerator-globaltool
reportgenerator "-reports:TestResults\*\coverage.cobertura.xml" "-targetdir:TestResults\coverage-report" "-reporttypes:TextSummary;Html"
type TestResults\coverage-report\Summary.txt

Depois na pasta do coverage, rode esse comando:

type Summary.txt

type TestResults\coverage-report\Summary.txt

start index.html   - Para inicar com um HTML PORÉM PRECISA ESTAR DENTRO DA PASTA COVERAGE-REPORT

dotnet test /p:CollectCoverage=true "Para criar automaticamente o TestResults"
cria: coverage-report\Summary.txt


-- Conexão com o Redis Dentro da pasta MinhaApi--
dotnet add package StackExchange.Redis
Coloca a conexão do Redis no docker-compose.yml (pedir ao gemini)
docker compose up -d
Depois colocar a conexão no appsetings.json para conexão com o redis (pedis IA)
Criar classes lista e colocar em uma pasta nova "Queue"
Incluir a saída do Redis no Controller.
Incluir chamada do Redis no program
docker exec -it redis_estudos redis-cli - verifica a URL, basta ir em Conectin database e trocar apenas os números e depois colocar para add que aparacem os POST no Redis.
Mexemos bastante no program.css e Controllers
docker exec -it redis_estudos redis-cli DEL lotes:processamento - deleta as listas do Redis

```






ExtraLotesMinerioControllers
## Como testar os endpoints:

### 1. **Obter classificação de um lote**
```
GET: https://localhost:5001/api/ExtraLotesMinerio/classificacao/1
```

### 2. **Listar todos com classificação**
```
GET: https://localhost:5001/api/ExtraLotesMinerio/listar-com-classificacao
```

### 3. **Filtrar por qualidade**
```
GET: https://localhost:5001/api/ExtraLotesMinerio/por-qualidade/Premium
```

### 4. **Apenas lotes Premium**
```
GET: https://localhost:5001/api/ExtraLotesMinerio/premium
```

### 5. **Estatísticas**
```
GET: https://localhost:5001/api/ExtraLotesMinerio/estatisticas
```

### 6. **Classificar um lote (POST)**
```
POST: https://localhost:5001/api/ExtraLotesMinerio/classificar
Body (JSON):
{
  "codigoLote": "MNA-2026-999",
  "teorFe": 66.0,
  "umidade": 7.5,
  "siO2": 2.8
}

```
PrecificacaoControllers
## Como testar os endpoints:

### 1. **Precificação de um lote específico**
```
GET: https://localhost:5001/api/Precificacao/lote/1
```

### 2. **Tabela de preços**
```
GET: https://localhost:5001/api/Precificacao/tabela-precos
```

### 3. **Resumo geral com valores**
```
GET: https://localhost:5001/api/Precificacao/resumo-geral
```

### 4. **Valores por classificação**
```
GET: https://localhost:5001/api/Precificacao/por-classificacao
```

### 5. **Potencial de ganho**
```
GET: https://localhost:5001/api/Precificacao/potencial-ganho
```

### 6. **Simular precificação**
```
POST: https://localhost:5001/api/Precificacao/simular
Body (JSON):
{
  "codigoLote": "TEST-001",
  "teorFe": 66.0,
  "umidade": 7.5,
  "siO2": 2.8,
  "toneladas": 10000
}

-- HistoricoMovimentacaoController -- 

```

## Como usar os endpoints:

### 1. **Ver histórico completo de um lote**
```
GET: https://localhost:5001/api/HistoricoMovimentacao/lote/1
```

### 2. **Ver rastreabilidade (timeline)**
```
GET: https://localhost:5001/api/HistoricoMovimentacao/rastreabilidade/1
```

### 3. **Filtrar por período**
```
GET: https://localhost:5001/api/HistoricoMovimentacao/por-periodo?dataInicio=2026-01-01&dataFim=2026-02-28
```

### 4. **Filtrar por tipo**
```
GET: https://localhost:5001/api/HistoricoMovimentacao/por-tipo/Embarque
```

### 5. **Registrar mudança de status**
```
POST: https://localhost:5001/api/HistoricoMovimentacao/registrar-status
Body:
{
  "loteId": 1,
  "novoStatus": 1,
  "usuario": "joao.silva@empresa.com",
  "observacoes": "Lote liberado para transporte"
}
```

### 6. **Registrar mudança de localização**
```
POST: https://localhost:5001/api/HistoricoMovimentacao/registrar-localizacao
Body:
{
  "loteId": 1,
  "novaLocalizacao": "EFVM - Trem 789",
  "usuario": "maria.santos@empresa.com",
  "observacoes": "Transporte iniciado às 08:00"
}
```

### 7. **Registrar embarque**
```
POST: https://localhost:5001/api/HistoricoMovimentacao/registrar-embarque
Body:
{
  "loteId": 1,
  "destino": "China",
  "numeroNavio": "Vale Beijing",
  "usuario": "embarque@empresa.com"
}
```

### 8. **Ver estatísticas**
```
GET: https://localhost:5001/api/HistoricoMovimentacao/estatisticas


-- FluxoLogistico ------------------------------------------------------------------------------------------

```

## 3. **Como usar os endpoints:**

### **1. Avançar status do lote**
```
POST: https://localhost:5001/api/FluxoLogistico/avancar
Body:
{
  "loteId": 1,
  "novaLocalizacao": "EFVM - Trem 456",
  "usuario": "joao.silva@empresa.com",
  "observacoes": "Iniciando transporte para o porto"
}
```

### **2. Retroceder status (exceção)**
```
POST: https://localhost:5001/api/FluxoLogistico/retroceder
Body:
{
  "loteId": 1,
  "novaLocalizacao": "Pátio Carajás",
  "usuario": "maria.santos@empresa.com",
  "motivo": "Problemas mecânicos no trem - lote retornado ao estoque"
}
```

### **3. Consultar próximo status**
```
GET: https://localhost:5001/api/FluxoLogistico/proximo-status/1
```

### **4. Ver fluxo completo**
```
GET: https://localhost:5001/api/FluxoLogistico/fluxo-completo
```

### **5. Visualizar progresso**
```
GET: https://localhost:5001/api/FluxoLogistico/progresso/1
```

### **6. Simular fluxo completo (para testes)**
```
POST: https://localhost:5001/api/FluxoLogistico/simular-fluxo-completo
Body:
{
  "loteId": 1,
  "usuario": "teste@empresa.com",
  "simularTempo": true
}

------------------ PENALIDADES CONTROLLERS --------------------------------

```

## 3. **Como usar os endpoints:**

### **1. Calcular penalidade de um lote**
```
GET: https://localhost:5001/api/Penalidades/lote/1
```

### **2. Ver tabela de penalidades**
```
GET: https://localhost:5001/api/Penalidades/tabela